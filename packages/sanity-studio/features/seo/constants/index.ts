export enum PAGE_VISIBILITY {
  PUBLIC = 'public',
  PRIVATE = 'private',
  HIDDEN = 'hidden',
}

export enum SEO_FIELD {
  META_TITLE = 'metaTitle',
  META_DESCRIPTION = 'metaDescription',
  SOCIAL_IMAGE = 'socialImage',
}

export enum WITH_SITE_TITLE {
  APPEND = 'append',
  PREPEND = 'prepend',
  OFF = 'off',
}
